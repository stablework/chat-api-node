# Leave Additional nginx directives EMPTY until
# https://donald.goodbusinessdev.com/ returns "Chat API".
#
# Extra location/proxy_pass/if blocks caused 502 by stealing
# /api/ away from Plesk Node.js. CORS is already handled in Node.
#
# After the API responds, keep Proxy mode as Plesk Node.js set it.
# Do not proxy to 127.0.0.1:3000 unless that is the port shown
# in Plesk → Node.js.
