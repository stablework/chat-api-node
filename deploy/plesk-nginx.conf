# Additional nginx directives must stay EMPTY.
#
# If login or /api/health fail while https://donald.goodbusinessdev.com/
# works, a leftover `location /api/` is sending API calls away from
# the Plesk Node.js process. Remove those directives, then:
#   plesk repair web donald.goodbusinessdev.com
#
# Keep Proxy mode enabled unless Plesk Node.js docs say otherwise.
# Do not proxy /api/ to 127.0.0.1:3000.
